# localdns cli

`localdns` is the cli for localdns server, it uses the gRPC API.

## commands

| command | description |
|---------|-------------|
| list    | -           |
| create  | -           |
| delete  | -           |
| get     | -           |
